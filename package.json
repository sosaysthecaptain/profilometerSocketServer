{
  "name": "socketserver",
  "version": "1.0.0",
  "description": "Simple server using WebSockets for communication between profilometer and client",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Marc Auger",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3",
    "socket.io": "^2.1.0"
  }
}
